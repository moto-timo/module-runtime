* mst would like fewer build-time deps, to install more easily on 5.8 where M:B is not bundled in core

* wishlist: should have something akin to compose_module_name that supports the evolved pseudostandard syntax used by DBIx::Class, Catalyst, Moose
